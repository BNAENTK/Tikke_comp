# 리그 앵커 발견 체크리스트 (2026-08-07)

배경·근거는 `context-notes-앵커발견.md`.

## 구현
- [x] `league-cutoff-harvester.ts` `fetchSeeds()` 출처를 `user_league_data` → `broadcaster_rank_snapshots`(오늘, region=kr)로 교체
      → 검증: D5(100) 후보가 **오늘 D5 인 사람**으로 나오는지. 어제 D5(=오늘 D4) 가 섞이면 실패
- [x] 오늘 행 0인 리그는 어제분 폴백 (D5 교착 대응)
      → 검증: 오늘 0행인 리그에서도 후보 목록이 비지 않는지
- [x] `SEEDS_PER_LEAGUE` 12 → 30
      → 검증: 리그당 라이브 앵커 4개 목표 달성률이 오르는지 (로그 `live anchor=` 개수)
- [x] 페이지네이션 — 스냅샷은 하루 4,000행 이상이라 limit 1000 한 번으론 하위 리그가 잘림
      → 검증: 18리그 전부 후보가 잡히는지 (리그별 후보 수 로그)

## 검증
- [x] `pnpm --filter @tikke/desktop typecheck`
- [ ] 앱 실행 후 `[harvester]` 로그에서 리그별 후보 수·라이브 앵커 수 확인
- [ ] 정지 중인 D3(300)·D1(500)·D2(400) 가 60분 안에 갱신되는지 실측
- [x] 기존 동작 회귀 없음 — 매치 상대 수집(`collectFromMatchHosts`) 경로는 건드리지 않음

## 배포
- [x] 커밋 (의미 단위 1개)
- [ ] 데스크탑 배포는 K 가 "배포해" 할 때만. 자동 진행 금지

## 하지 않는 것
- 새 서비스 신설 (harvester 가 이미 발견·폴링·보고를 다 한다)
- 별도 건전성 엔드포인트 (정기점검 B 신선도가 이미 드러냄)
- 외부 대조 사이트·voxo·tik.tools 연동 (경쟁사 의존 + 유료 게이팅)

## 실측 (2026-08-07)
- 시드 쿼리 결과: 18리그 전부 후보 확보, 후보 0인 리그 **0**.
  A1~D4 는 오늘 명단(30명씩, D3만 23명), **D5 만 어제 폴백** — 오늘 0행이라 설계대로.
- 남은 검증: 앱 재빌드 후 실제 앵커 발견률과 D3·D1·D2 회복은 배포 뒤에야 측정 가능.
