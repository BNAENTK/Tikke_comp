# Chatterbox Multilingual TTS 서버 의존성
# Python 3.10+ 권장

# 서버 프레임워크
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# TTS 모델 (Resemble AI Chatterbox — zero-shot 다국어 보이스 클로닝)
chatterbox-tts

# 오디오 처리
soundfile>=0.12.1
numpy>=1.24.0

# 부모 프로세스 PID 모니터링 (좀비 방지 watchdog)
psutil>=5.9.0

# 환경변수 관리 (선택)
python-dotenv>=1.0.0

# PyTorch — CUDA 버전은 별도 설치 필요 (chatterbox-tts 가 CPU wheel 로 덮어쓸 수 있으니
# torch 를 먼저 CUDA 버전으로 설치한 뒤 chatterbox-tts 를 설치할 것)
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu121
torch>=2.1.0
torchaudio>=2.1.0
