# 리그 조각컷 능동 수집기 (League Cutoff Harvester) — 설계 + 체크리스트

## 배경 / 문제
- 현재 컷오프 수집은 `tiklive.ts`가 **사용자가 연결된 방 1개**만 3분 주기로 수집(`collectLeagueCutoff`). 앱 닫거나 방송 끝나면 정지 → 워커 30분 cron은 죽은 풀 방을 찔러 gap 없음 → **값이 마지막 수집 시각(예: 새벽 1시)에 얼어붙음.**
- 목표는 18리그(A1~A3, B1~5, C1~5, D1~5) 전부 실시간. 현재 tikke는 구조(리그·퍼센타일)는 갖췄으나 **stale + 단발 쓰레기값**.
- serve 측 쓰레기값은 이미 수정됨(`cutoffs.ts` 퍼센타일별 MAX + 단조성, 커밋 76b7037). **남은 건 freshness** = 이 문서.

## 검증된 프리미티브
- **시드**: `user_league_data`(Supabase)에 `unique_id`(@핸들) + `user_id` + `class_type` + `score` 누적. 저리그 넉넉(400=24), **고리그 thin(1800=2, 1500=1, 800=1)**.
- **발견**: `room-observer.ts::fetchRoomSnapshot(uniqueId)` → `fetchIsLive()`(line103) + api-live에서 `roomId`(line124). anchorId = user_league_data.user_id. → uniqueId→(live, roomId, anchorId) 해석 가능.
- **수집**: `league-cutoff-collector.ts::collectLeagueCutoff(sessionId, roomId, anchorId, displayId)` 그대로 재사용(additive — 같은 upsert→serve max-pick 경유, 쓰레기 재유입 불가).

## 아키텍처 (호스트 무관 단일 모듈)
신규 `apps/desktop/electron/services/league-cutoff-harvester.ts` — 데스크탑(2)·상시호스트(3) 공용 로직.

상태: `Map<class_type, { room: {roomId,anchorId,uniqueId}|null, liveCheckedAt:number }>`

**2개 루프 (레이트 분리 — TikTok bot/429 방지, room-observer 위생 재사용: 호출 간 500ms+, 에러 dedupe, 백오프):**
1. **발견 루프** (느림, TTL 기반): 리그가 known-live 방 없거나 TTL(20~30분) 만료 시에만. 해당 리그 시드(user_league_data top score, fresh)를 순회 → `fetchRoomSnapshot` → **첫 라이브에서 중단**(전수 탐색 금지). room+anchor 확정 저장.
2. **폴링 루프** (3~5분): known-live 방마다 `collectLeagueCutoff`. 실패(방 죽음) → room=null → 다음 발견 루프가 재탐색.

**시드 자가치유(2b, ranks 수확)**: `collectLeagueCutoff` 성공 시 `rank_view.ranks[].display_id`를 `user_league_data` 시드로 upsert → 라이브 앵커 1명이 그 리그 top-N 시드 부트스트랩 → thin 고리그 점진 해결.
- ⚠️ 선결 검증: rank_type=16 응답의 `rank_view.ranks`에 `display_id` 실재하는지 확인(rank_type=8과 동일 shape 추정, 미확인). admin secret 필요한 `/league/cutoff-test`로 K가 확인하거나 런타임 로그.

## 정직한 한계
- 그 순간 **라이브 앵커가 0인 리그는 여전히 못 받음.** 시간이 지나며 수렴(자가치유)이지 day-1 동급 아님.
- sign-api 429(EulerStream) 리스크 — tiktokSignApiKey 설정 필요([[reference_tikke_sign_api]]).

## 단계
- **(2) 데스크탑 내장**: 모듈 + 앱 시작 시 기동. 앱 열려있는 동안 18리그 fresh. ← 1차
- **(3) 상시 호스트**: 같은 모듈 헤드리스 진입점 + 호스트 + secret(sessionId·signKey, 하드코딩 금지). Node 런타임 필요(Worker 불가). **후보: Tikketone 상시 서버**([[project_tikketone]]). ← 호스트 결정 대기

## 체크리스트
- [x] harvester 모듈 작성 (발견/폴링 2루프, 상태, 위생) — `league-cutoff-harvester.ts`
- [x] Supabase 시드 fetch 헬퍼 (user_league_data, 점수순 리그당 6개)
- [x] fetchRoomSnapshot → roomId 확보 (anchorId=seed.user_id)
- [x] collectLeagueCutoff 반환 boolean화 (방 죽음 감지)
- [x] 데스크탑 startup 기동 (sessionId 있을 때) — main/index.ts
- [x] typecheck (desktop 통과)
- [ ] **K 검증**: 내가 연결 안 한 고리그(1800~2000)에 수 분 내 fresh row 생기는지 (라이브 필요 — 헤드리스 불가)
- [x] (2b) ranks 수확 — `harvestSeeds` in league-cutoff-collector.ts, ignore-duplicates(기존 안 덮음). ⚠️ rank_view.ranks display_id 실재는 라이브 로그로 확인 필요(방어적 파싱이라 없어도 무해)
- [x] (3-lite) 워커 cron 강건화 — 풀 후보 3→8, 풀 상한 5→8. harvester 발견 방을 워커가 24/7 headless(md5 서명) 폴링
- [ ] (3-full) 데스크탑 완전 독립 24/7 — harvester가 Electron 의존(net/getSetting/captureRankRaw=히든브라우저). 비-Electron 호스트엔 (a)Electron-on-server 또는 (b)워커에 api-live 발견 포팅 필요. **아키텍처 결정 대기.**

## 진행 메모 (2026-06-27)
2(데스크탑) 핵심 완성·타입통과. 앱 켜져있는 동안 18리그 능동 수집. 다음: K 라이브 검증 → 2b(ranks 자가치유) → 3(호스트 결정: Tikketone 서버 후보).
파라미터: 발견 TTL 25분, 폴링 4분, 호출간격 700ms, 리그당 시드 6개.

## 검증 기준 (헤드리스 테스트 불가 — 라이브 TikTok)
"내가 안 보는 리그에 fresh 값" = 원 불만 직결 성공 기준. typecheck로는 불충분.

## 진행 메모 (2026-07-06) — 퍼센타일 커버리지 개선
- **방향**: 외부 미러는 배제(원천이 어차피 동일 TikTok rank_type=16이라 자체수집으로 충분). 자체수집 강화에 집중.
- **근본 병목 발견**: 앵커 1명 = 퍼센타일 1개(owner_rank.gap의 targetPercentile, 앵커 위치가 결정). 기존 harvester는 리그당 **점수 top-6**만 시드+**첫 라이브 1명서 중단** → 전부 최상위 → **1%만 수집, 20/60/70/80 영영 못 잡음.**
- **수정(harvester 단일 파일)**: ①`stratifiedSample` — class별 점수 분포 전체서 균등 추출(상위~하위) → 다양한 순위. ②`SEEDS_PER_LEAGUE` 6→12. ③리그당 라이브 앵커 1→`LIVE_ANCHORS_PER_LEAGUE`=4 유지(`live[ct]`=배열). ④`discoverLeague` 최대 4명 수집, `runPoll` 전부 폴링·생존만 유지.
- **워커 자동 혜택**: `reportRoomToWorker`가 다양한 방 등록 → 워커 30분 cron(항상 켜짐, 풀 8/class)도 다양한 퍼센타일 수집. 워커 코드 무변경.
- 부하: 18리그×4=72콜/4분, 700ms 간격 = ~50초/사이클. typecheck 통과, stratifiedSample 단위검증(100명→0,9,…,99 균등).
- **K 검증 대기(라이브 필요)**: 한 리그에서 여러 퍼센타일 행(1/10/20/60 등)이 수 분 내 생기는지 Supabase에서 확인.
파라미터 갱신: 시드 6→12, 리그당 라이브앵커 1→4.

## 진행 메모 (2026-08-02 새벽) — 서빙 정확도 3건 수정 + 기각 1건

배포: v1.8.213 / v1.8.214 / v1.8.215 (worker). 대상 파일 `apps/worker/src/routes/cutoffs.ts`.

### 1. 비자정 회차 행 배제 (`isOffRoundRow`) — v1.8.213
리그 회차는 KST 자정 종료인데 `round_end_at`이 13:00/06:00/08:00인 행이 섞인다(오늘 46행 중 8행).
A1 20%에 116,847이 들어가 10%컷 59,656보다 큰 **역전**을 만들었다. 자정이 아닌 회차 행을 배제.
`round_end_at`이 null인 옛 행은 판단 보류(그대로 신뢰).
- 부작용: A리그 신선 행 4개가 함께 배제된다(A1 20%, A2 20%·60%, A3 60%). 이들은 전부 비자정이었다.
- 근거: 같은 리그 안에서 자정/비자정이 섞이므로 "A리그는 회차가 다르다"가 아니라 파싱이 흔들리는 것.
  B1 교차검증에서 비자정 행 10%=53,419는 순위표 23,536의 **2.27배**로 실제 틀린 값이었다.

### 2. 이상치 상한 환산 (`capAt`) — v1.8.214
`interpAt`은 어제 곡선 범위 밖을 **평평하게 클램프**한다. 어제 곡선이 {20%:24}뿐이면 10% 상한도 24가 되어
허용 최대 72 → 오늘 실측 116이 이상치로 버려졌다(D5). 순위표 독립 계산은 123이라 116이 맞았다.
범위 밖은 그룹 곡선 비율로 환산 후 비교. 비율을 모르면 상한을 아예 안 건다(틀린 상한이 더 나쁨).
- A/B(HEAD vs 수정, 동일 실데이터): **칸 +3 / -0 / 역전 0→0**. D1 10%, D5 10%·20% 신규 확보.
- B5 1% 54,723 → 109,999. 기존 값은 실측이 버려진 뒤 순위표 역산으로 채운 **추정치**였고 새 값은 실측.
  이 파일 원칙 "순위표는 실측을 덮지 않는다"에 부합.

### 3. 역전 제거를 최장 비증가 부분수열로 (`dropInversions`) — v1.8.215
처음엔 낮은 %부터 그리디로 기준선을 잡았는데, **그 값이 낡으면 뒤의 정상값을 전부 죽인다**.
실측 A2: 낡은 10%=3,313(00:16)이 같은 시각 들어온 20%=65,100·60%=16,061을 둘 다 버렸다.
→ 서로 모순되지 않는 칸이 최대로 남는 조합 선택. 동률이면 낮은 % 우선(A1 케이스 기존 동작 보존).
검증 10케이스 통과(실측 역전 2건 + 정상 곡선 3건 + 경계).

### 4. [기각] 순위표 최하위 하한 규칙
"상위 구간(≤20%) 컷이 순위표 최하위보다 낮으면 배제" — A/B에서 **6칸 손실**.
D2 10%=222·20%=113은 D3(300/109)·D4(184/46)와 일관된 정상값인데 함께 날아갔다.
전제(순위표 = 그 리그 상위 L명)가 A리그에서 깨진다. 상세는 아래.

### 미해결 — A리그 순위표 모집단 불일치
`broadcaster_rank_snapshots`와 `league_cutoffs`가 A리그에서 다른 모집단을 본다.
- A1 순위표 최저 222,541인데 컷 측정 앵커는 rank 91 / score 36,556 (1/6 수준)
- A2 순위표 최저 6,050인데 10% 컷 실측 3,313 (목록 최하위보다 낮음 → 1,040명+ 필요, B1 137명이라 불가)
- B/C/D는 정상: B1 20% 실측 15,906 vs 순위표 15,273 = **1.04배**
→ `estimateLeagueSize`가 A리그에서 항상 null → 순위표 보강·검증이 A만 작동 안 함.
**다음 단계**: A리그 `rank_view` 원본에서 `ranks[]`가 리그 내 순위가 맞는지 확인.

### 확인됨 — `전날 동시간` 빈칸은 정상, 낮에 채워짐
새벽엔 리그 중위권 앵커만 방송 중이라 틱톡이 알려주는 목표%가 43%/65% 같은 **비캐논**뿐이다.
어제 캐논 % 수집: 00~09시 **0건**, 10시 2, 12시 5, 21시 4, 23시 31.
→ 코드 문제 아님. 오전 10시부터 자연히 채워진다. 순위표로 대체하려면 시각별 이력이 필요한데
`broadcaster_rank_snapshots`는 `UNIQUE (broadcaster_unique_id, snapshot_date)`로 하루 한 행 덮어쓰기라 불가.
**이력 테이블은 만들지 않기로 함** — 채울 수 있는 값이 전부 순위표 역산(=추정)이라 "추정은 안 됨" 지시에 어긋난다.

## 진행 메모 (2026-08-02 오전) — 한국 리그 오염 원인 규명 및 차단

배포: v1.8.216 ~ v1.8.219. 대상: `apps/worker/src/lib/rank-cron.ts`, `routes/cutoffs.ts`.

### 근본 원인 — 리그 순위표·컷은 지역(국가)별로 갈린다
해외 앵커를 프로브하면 그 지역 순위표와 그 지역 컷이 한국 리그 것으로 저장된다. 게다가 되먹임 고리가 있다.
```
fetchRankProbeTargets (broadcaster_rank_snapshots에서 대상 선정)
   → fetchClassRankRaw (그 사람 지역 순위표)
   → saveLeagueRanks (같은 테이블에 저장) ──┐
   └───────── 다음 사이클의 프로브 대상 ←──┘
```
`harvestRanksAsSeeds`도 같은 고리를 `user_league_data`에 먹인다. **해외 1명 유입 = 몇 사이클 만에 리그 전체 전환.**

**확정 증거(K 스크린샷 대조)**: 우리 A1 1위 Tyler River 2,197,445 vs 실제 한국 A1 1위 585,100.
A1 컷 1% = 842,769 — 실제 1위 점수보다 높은 불가능한 값이었다.
B·C·D는 Tikke 사용자가 방송해 한국 유지(히소·마이규미·융데·당여원·시블리·문찌).
**한국 사용자가 0인 A리그만 넘어갔다.**

### 진단 요령 (재발 시)
`updated_at`이 같은 행 = 한 프로브의 순위표 = 한 지역. 크론 1회당 리그별 첫 프로브의 99명만 저장되므로
(`ranksSaved` 가드) **그 첫 프로브의 국적이 리그 전체를 결정한다.** 배치별 상위 닉네임만 훑으면 즉시 보인다.
`updated_at` 유무로는 못 가른다 — A1은 신선분이 해외/옛행이 한국, A2는 정반대였다.

### 해결
- `broadcaster_rank_snapshots.region` 추가. **`region='kr'`만 저장·프로브·서빙.**
- 신뢰 시작점: Tikke 사용자 등록 방(`src='user'`) + 데스크탑 연결 방. 한국 앵커의 순위표 99명 전원 kr 전파.
- `isKr()` 게이트를 upsertCutoff / saveLeagueRanks / harvestRanksAsSeeds 3곳에 적용.
- `KR_BOOTSTRAP_HOSTS` — 신뢰는 기존 한국 앵커에서만 번지므로 한국 앵커 0인 리그는 영영 못 산다.
  K가 방송 확인해 준 계정을 명시적 시작점으로 둔다. A1: 000819_(연꿍💡), hu0o0oh, rjslr23(L엘🎈),
  koreano767(☘️KEN☘️) / A2: ryuperto(Ryuperb) / A3: like_hajihyo(🪇지효). **K 확인 없이 추가 금지.**
  리그는 라이브 응답 `class_extra.class_type`에서 읽는다 — `user_league_data`의 리그 값은 한 달씩 정체돼 있다
  (연꿍 A1인데 B1로, ryuperto A2인데 B3로, like_hajihyo A3인데 B2로 기록돼 있었다).

### 실수 1건 — backfill 범위 (v1.8.218에서 수정)
최초 backfill을 `snapshot_date >= today-1`로 걸어 **어제의 미검증 해외 행까지 kr로 표시**했다.
`fetchKrAnchors`가 2일을 조회해 그 해외 앵커가 신뢰 집합에 들어갔고, 한 사이클 만에 A1·A3가 다시 뒤집혔다.
→ `fetchKrAnchors`를 **당일만** 조회하도록 좁히고 과거 날짜 region을 회수했다.
날짜가 바뀌어 당일 kr이 비면 신뢰는 부트스트랩 호스트와 `src='user'` 방에서 다시 시작한다(의도된 경로).

### 정리한 데이터
순위표 705 + 201행, A1·A3 오늘 컷오프 8건 삭제. 과거 날짜 region 회수.

### 남은 확인
- 데스크탑 `league-cutoff-harvester.ts`도 같은 시드에서 수집한다 — 동일 게이트 필요 여부 미검토.

## 진행 메모 (2026-08-02 오후) — 지역 오염 최종 해결 + 외부 대조 사이트 실물 대조

배포 v1.8.216~v1.8.232. **핵심 교훈: 이 파이프라인은 셀 개수로 검증하면 안 된다. 배치별 닉네임으로 본다.**

### 컷 계산법 확정 (외부 대조 사이트 /ranking/a1 실물 대조)
`컷(p) = 순위표를 점수 내림차순 정렬했을 때 ceil(N × p/100)번째 점수`
- 외부 대조 사이트 A1: 1%=481,883 / 10%=131,939. 우리 데이터의 6위=481,882, 60위=131,938 — **일치**.
- 6위=1%, 60위=10% → **A1 인원 N ≈ 600**. 20%컷 71,369가 우리 목록 119위 → 119/0.2=595로 교차 확인.
- 즉 안내 문구 역산 없이 순위표만으로 전 구간 계산 가능. 못 했던 건 N 산출 실패 때문이지 데이터 부재가 아니다.
- 우리 A1 순위표는 정확하다: woon 1,667,822 / L엘 734,544 / Siwon 585,170 (외부 대조 사이트와 동일).

### 오염 구조와 최종 방어
되먹임 고리: `순위표 → 프로브 대상 → 순위표`. 입구는 **기준선이 없는 리그의 최초 수립**이다.
1. `region='kr'` 컬럼 — kr만 저장·프로브·서빙
2. 명단 겹침 — 새 목록이 그 리그 기존 한국 명단과 15명 미만 겹치면 폐기
3. 점수 규모 — 기존 한국 최고 대비 3배 밖이면 폐기
4. **기준선 최초 수립은 `KR_BOOTSTRAP_HOSTS`만** ← 이게 결정타
   사용자 등록 방·데스크탑 연결 방에도 수립 자격을 줬더니 A3가 세 번 연속 다른 해외 목록에
   선점당했다(아랍권 → Jin Gates → Maria). 그 경로는 매 크론 폴링되는데 부트스트랩 계정은
   방송을 쉬는 때가 많아 리그가 비는 순간 경주에서 진다. 수립을 확인 계정으로 좁히자 A3 복구됨.

### KR_BOOTSTRAP_HOSTS (K가 방송 확인해 준 계정만 추가할 것)
A1 000819_(연꿍💡) hu0o0oh rjslr23(L엘🎈) koreano767(☘️KEN☘️)
A2 ryuperto(Ryuperb)  A3 like_hajihyo(지효) bbois1004 dbwjddlek_(유정) gkgkaos09(Soo)
B2 nineone2828(GUWON)
※ 리그는 라이브 응답 class_extra.class_type에서 읽으므로 주석의 리그가 달라도 무해.
※ **여기가 오염되면 전체가 오염된다. K 확인 없이 추가 금지.**

### 내가 틀린 판정들 (같은 실수 반복 금지)
- 칸이 채워진 걸 정상으로 판단 → 해외 데이터가 채운 것이었다
- 재오염을 배포 타이밍 탓으로 오판 → 실제로는 게이트 결함이었다(배포 시각 대조로 갈림)
- 닉네임으로 A3 국적 판정 → 근거 부족
- **점수 규모의 리그 간 순서로 판정 → 지역이 다르면 성립하지 않는다**(A3를 반대로 판정했다)
- woon 점수 급등(585K→1.48M)을 오염으로 의심해 정상 A1 회수 → 외부 대조 사이트 대조로 정정
전부 실물 대조 없이 대리 지표로 판단했을 때 생겼다.

### 현재 상태
한국 확정 11리그: A1 A2 A3 B1 B2 B3 B4 C2 C3 (+D 일부)
빈 리그: B5 C1 C4 C5 D1~D5 — **부트스트랩 계정 확보 시 즉시 해소**
서빙 51/58칸, 역전 0. 결측 5칸이 위 빈 리그의 10% 구간이다.

## 2026-08-07 · 위치 겨냥이 상위 400명 안에서만 계산되던 결함

증상: A2 유지칸(60%)이 비어 있다. 7일 중 정상 수집 A1 5일 / A3 4일 / **A2 2일**.

서빙은 무죄다. `dropped` 진단이 비었고, 오늘 A2 60% 행은 딱 하나인데
`round_end_at = 2026-08-08 04:00Z`(KST 13:00) 즉 비자정 회차라 `isOffRoundRow`가 뺐다.
값도 5,600(owner_score 0, 20시 시점)으로 A3 60%(8,563)보다 낮아 어차피 못 쓴다.

진짜 원인은 수집 겨냥이다. `fetchTargetedSeeds`의 임계-미확보 분기가
`list.length * p / 100`으로 자리를 잡는데 `list`는 `limit=400`으로 자른 상위 400명이다.
p=60 → 240위 → 전체 3,429명 기준으로는 **7% 자리**. 실측 대조:

| 리그 | 전체 | 옛 픽 점수 | 새 픽 점수 | 그날 컷 |
|---|---|---|---|---|
| A1 60% | 3,298 | 303,106 | 8,817 | 20,217 |
| A2 60% | 3,429 | 88,983 | 5,388 | (10%컷 63,456) |
| A3 60% | 3,396 | 80,968 | 6,162 | 8,563 |
| B1 70% | 4,591 | 76,446 | 2,997 | — |
| C3 80% | 1,087 | 5,014 | 127 | — |

옛 픽은 전부 10%컷보다 위였다. 그런 앵커에게 TikTok은 1% 목표를 안내하므로
유지컷은 영영 안 나온다. 2026-08-04에 위치 겨냥을 넣고도 유지칸이 계속 비었던 이유.

수정: `Prefer: count=exact`로 리그 전체 인원을 받아 `total * p / 100`으로 자리를 잡고,
그 자리가 상위 400명 밖이면 `offset` 조회(`fetchSeedsAt`)로 3명만 따로 떠 온다.

터질 수 있는 범위는 좁다 — 하베스터는 **어느 앵커를 폴링할지**만 고르고 퍼센타일은
TikTok의 `owner_rank.gap`이 정한다. 빗나가면 캐논 밖 행(오늘의 48%/49%)이 하나 쌓이고
서빙이 캐논 매칭으로 무시한다. 잘못된 캐논 값을 쓸 수는 없다. 최악이 또 빈칸이다.

검증 한계: typecheck는 이 수정을 검증하지 못한다. 진짜 확인은 다음 크론 이후
A2 60%가 자정 회차로 들어오는지다.

남은 것: `isOffRoundRow` / `isPastRoundRow`는 `reconstruct` 전에 거르므로 `drops`에 안 남는다.
그래서 이번 빈칸은 진단 흔적이 없었다. 여기도 사유를 남기면 다음 "왜 비어있어"가 API 한 번으로 끝난다(별건).

### 보강 — 유지컷에는 낡은 임계를 쓴다 (K 제보: A2 유지컷 22.7K)

위치 겨냥만으로는 부족했다. 풀 퍼센타일과 실제 리그 퍼센타일이 안 맞는다 —
A2 실제 60%컷 22,747 은 `user_league_data` 풀의 **41.6%** 자리인데 풀 60% 자리는 5,388 이다.
풀이 `rank_view.ranks`(상위 명단)에서 수확돼 고득점 쪽으로 치우친 탓이다.
그만큼 내려가면 A 리그는 60% 가 아니라 강등선(99%) 안내를 받을 수 있다.

그런데 어제값 22,747 은 우리가 이미 갖고 있다. 신선도 필터(`CUTOFF_STALE_MS` 3시간)가
겨냥에서 뺐을 뿐이다. 그 필터의 근거는 "낡은 임계 바로 아래 앵커는 지금은 더 쉬운 구간에 있어
그쪽 안내를 받는다"인데(B4 10% → p=20 반복), **유지컷은 이미 그룹에서 가장 쉬운 캐논이라
밀릴 곳이 강등선뿐이고 그건 캐논 밖이라 서빙이 무시한다.** 그래서 유지컷(그룹 최대 %)에만
낡은 임계를 허용한다.

18리그 전부 임계 보유 확인, 앵커가 문턱 바로 아래에 붙는다.

| 리그 | 유지컷 | 새 앵커 |
|---|---|---|
| A1 60% | 46,768 | 46,744 |
| A2 60% | 22,747 | 22,711 |
| A3 60% | 17,400 | 17,359 |
| B1 70% | 12,947 | 12,928 |
| C1 80% | 3,200 | 3,199 |
| D1 20% | 1,043 | 1,036 |

위치 겨냥(전체 인원 기준)은 임계가 아예 없는 신규 리그용 최후 폴백으로 남는다.

## 진행 메모 (2026-08-28 02:35 KST) — 리그컷 대조

### 외부 대조는 끊겼다 — tikdo 전면 로그인 게이트
- 크로스 오리진 API 호출 → 403 `api_cross_site_request`
- 브라우저(Firecrawl stealth, WAF 통과)에서 same-origin 으로 18리그 fetch → 전부
  `{"error":"unauthorized","message":"로그인이 필요합니다."}`
- `/ranking/*`, `/history/ranking/*` 본문도 "로그인 후 확인" 안내만 렌더. curl 은 여전히 429.
→ 익명 외부 대조 경로 없음. 남은 건 K 구글 로그인 세션에서의 same-origin fetch 뿐(미실행, 승인 대기).
→ `league_cutoffs` 에 `source='ext'` 행은 현재 0건(테이블은 08-22~08-28 7일치, 전부 `source='tikke'`).

### 자체 대조 (raw 행 vs `api.tikke.kr/league/cutoffs`)
서빙 캐논 53/58칸, 역전 0. **차이는 아래 6칸뿐이고 나머지는 raw == 서빙 일치**
(A1 10%/20%, B4 10%, C2 10%, D2 10%, D4 20% 등 spot check 전부 정확히 일치).
dropped/served 는 같은 응답에서 나왔으므로 스냅샷 시차 영향 없음.

버린 실측 3칸:
| 리그 | % | 버린 실측 | 사유 | 대신 나간 값 |
|---|---|---|---|---|
| A2 | 1% | 254,120 | 정합성 이탈(역산 200 vs 기준 950) | 68,576 |
| C1 | 10% | 5,801 | 낡은 값이 신선 실측과 모순 | 12,573 (**2.2배**) |
| C4 | 10% | 2,514 | 낡은 값이 신선 실측과 모순 | 6,278 (**2.5배**) |

- **A2 1% 는 매 사이클 기각된다** — 첫 조회 248,467, 6분 뒤 254,120, 둘 다 같은 사유.
  단발이 아니라 계통적이다. 검증식이 빡센 건지 앵커가 실제로 나쁜 건지는 미확인.
- C1/C4 대체값은 순위표 파생인데 실측의 2.2~2.5배다. 배수가 핵심이다.

실측 행 없이 서빙된 칸(순위표 파생): D1 10% 1,225 · D3 10% 944.
D3 10% 가 어제(311) 대비 2.5배로 보이던 건 이것 때문이다.

조용히 사라진 칸: D5 20% raw 1,000(anchor_rank 0, owner 0, 15:06Z, 어제 31 → 32배).
서빙에 안 나갔고 `dropped` 에도 없다. `round_end_at` 이 자정이라 `isOffRoundRow` 는 아니다.
**어느 필터인지 미확인** — 후보는 STALE_ANCHOR_MS(수집 후 2.5시간) 또는 OUTLIER_X(어제 대비 3배 상한).
`makeUnrankedMaintFilter` 는 아니다(어제 같은 시각 기준 없고 nowMinRef<180 이라 문턱 0 → 통과).

### 빈칸은 정상 — 시각 때문이다
A1/A2/A3 60%, D5 10%·20% 가 빈 것은 02:30 KST 라서다. 이 파일 기존 관측대로 캐논 % 수집은
00~09시 0건이다. 같은 이유로 18리그 전부 `yesterdaySameTime` 이 비어 있다.
today/yesterday 비율(0.2~0.5)도 회차 2.5시간 시점이라 비교 불가 — 지표로 읽지 말 것.

### 해소됨 — B그룹 1% 컷이 순위표 1위와 같은 이유
관측: B1 77,036 vs 1위 76,986(+50) · B3 169,790 vs 169,777(+13) · B4 77,087 vs 77,039(+48)
· B5 119,859 vs 119,857(+2) · B2 210,725 vs 210,941(**-216**).

TikTok 은 앵커가 아직 못 넘은 문턱만 안내한다(모든 행이 `cutoff_score > owner_score`).
따라서 목표 자리는 앵커 순위보다 위 → `0.01·p·N < anchor_rank`, 그리고 `N >= anchor_rank`.
유지컷 행의 `anchor_rank` 로 인원을 조인다:

| 리그 | 유지 % | anchor_rank | 인원 N | 1% 자리 | 관측 |
|---|---|---|---|---|---|
| B1 | 70% | 107 | 107~152 | 1위 | 1위 점수와 일치 ✓ |
| B2 | 70% | 107 | 107~152 | 1위 | 일치 ✓ |
| B5 | 70% | 125 | 125~178 | 1위 | 일치 ✓ |
| A1 | 50% | 340 | 340~680 | 4~7위 | 310,364 < 1위 383,766 ✓ |
| A2 | 50% | 194 | 194~388 | 2~4위 | 68,576 ≪ 1위 ✓ |
| A3 | 50% | 150 | 150~300 | 2~3위 | 125,337 < 126,668 ✓ |

B 리그는 인원이 100~180명이라 상위 1% = 1위 본인이다. **버그 아님.**
B2 가 음수(-216)인 것도 같은 근거다 — 문턱이 1위의 움직이는 점수를 따라다닌다(첫 조회 +23 → 두번째 -216).
(B3·B4 는 유지컷 앵커가 순위 0 이라 이 계산에서 제외. C 그룹은 1% 가 캐논이 아니다.)
`cutoffs.ts` L117 의 "1%만 N≈4600" 은 다른 역산이라 반증이 아니다.

## 진행 메모 (2026-08-28 02:50 KST) — 외부 대조 실행 (K 로그인 세션)

K 승인 후 K 크롬(구글 로그인 상태)에서 tikdo same-origin fetch 로 18리그 컷 전량 확보.
양쪽 다 17:46~17:47Z 값이라 시차 없음.

### 결과 요약 — 52칸 중 43칸이 10% 이내 일치
값 자체는 신뢰할 만하다. 벌어진 칸은 대부분 **우리 측정 시각이 낡아서**다(괄호가 우리 측정 시각).

| 칸 | tikke | tikdo | 차 | 우리 측정 |
|---|---|---|---|---|
| A2 1% | 58,805 | 286,331 | **-79%** | 17:46 (파생) |
| C1 10% | 13,159 | 20,993 | **-37%** | 17:46 (파생) |
| A1 20% | 23,787 | 38,835 | -39% | 16:37 |
| B2 10% | 19,877 | 28,890 | -31% | 16:13 |
| A3 10% | 15,319 | 20,835 | -27% | 16:17 |
| A3 1% | 79,880 | 100,324 | -20% | 17:46 |
| B1 70% | 504 | 426 | +18% | 17:41 |
| B5 10% | 15,136 | 18,046 | -16% | 17:01 |
| B3 70% | 318 | 362 | -12% | 17:43 |

- 낡은 실측(16:13~17:01)이 원인인 칸은 신선도 문제지 정확도 문제가 아니다.
- **A2 1% 와 C1 10% 는 신선한데도 크게 틀렸다.** 둘 다 실측을 버리고 순위표 파생값을 내보낸 칸이다.
  - A2 1%: 우리가 매 사이클 기각하는 실측(254,120)이 tikdo(286,331) 대비 -11% 로 오히려 가깝다.
    **`정합성 이탈` 판정이 정상값을 죽이고 있다.** 파생 대체값은 -79%.
    단 판정식을 느슨하게 푸는 건 오답일 수 있다 — 사유는 `역산 200 vs 기준 950, 허용 4배`인데
    A2 인원은 유지컷 앵커 순위로 조이면 194~388 명이라 **역산 200 쪽이 맞다. 기준 950 이 낡았거나 틀렸다.**
    고칠 대상은 문턱 배수가 아니라 기준값 산출이다.
  - C1 10%: 버린 실측 5,801 은 확실히 낮았지만(-72%), 대체한 파생값 13,159 도 -37% 다.

### 🔴 A그룹 유지컷 퍼센타일이 60% → 50% 로 바뀌었다 (우리만 못 받고 있음)
tikdo 는 A1/A2/A3 에 **50%** 를 서빙한다(8,689 / 2,329 / 2,944).
우리 raw 에도 오늘 50% 행이 있다(8,347 / 2,302 / 2,968 — tikdo 와 일치).
그런데 `CANON_GROUPS` 가 A 를 `[1,10,20,60]` 로 고정해서 **50% 행을 비캐논으로 버린다.**

DB 근거 (A그룹 퍼센타일 출현):
| 날짜 | 50% | 60% |
|---|---|---|
| 08-22 ~ 08-26 | 없음 | 3리그 전부 |
| 08-27 | 3리그 전부 | 3리그 전부 |
| 08-28 | 3리그 전부 | **없음** |

08-27 이 전환일이고 오늘부터 60% 는 아예 안 온다. 독립 수집자인 tikdo 도 50% 를 쓴다.
→ **어제 메모에서 "A 60% 빈칸은 02:30 이라서"라고 쓴 건 틀렸다.** 시각 문제가 아니라 캐논 불일치다.
이대로 두면 A 유지컷 3칸은 낮이 돼도 영영 빈칸이다.

수정안(미적용, K 승인 대기): `CANON_GROUPS` A 항목을 `[1,10,20,50]` 로 바꾼다. 한 줄이다.
회차마다 바뀌는 걸 견디게 하려면 **50·60 둘 다 캐논 허용 + 그 회차에 실제로 온 쪽 선택**까지가 상한이고,
**"그 회차 최대 % 를 유지컷으로" 는 절대 안 된다** — 08-27 A1 에 77/78/80% 쓰레기 행이 있었고
(407,580 / 413,003 / 409,052, 20% 컷 125,267 의 3배) 그 규칙이면 409,052 가 유지컷으로 나갔다.
고정 배열이 막고 있던 게 정확히 이거다.

값 검증(적용 전 확인): A1 50% 8,347 < 20% 23,787 · A2 50% 2,302 < 20% 13,728 · A3 50% 2,968 < 20% 14,055
— 단조 정상. 셋 다 tikdo 대비 ±4% 이내. 상수만 바꾸면 3칸이 바로 정상값으로 채워진다.

### 그 외
- D5 10%/20%: tikdo 194 / 54. 우리는 빈칸. 어제 우리가 걸러낸 D5 20% 실측 1,000 은 **버린 게 맞았다**(실제 54).
- D1 80%: 우리만 서빙(6). tikdo 는 D1 에 80% 를 안 쓴다. 캐논 재확인 대상.
- B1 70% 는 우리가 +18% 높다(504 vs 426). 유지컷 계열은 양쪽 다 흔들린다.

## 진행 메모 (2026-08-28 03:20) — A 유지컷 50% 전환 적용 (미배포)

### 무엇을 고쳤나
캐논 퍼센타일 표가 **5곳**에 흩어져 있었다. A 를 한 곳만 고치면 나머지가 조용히 어긋난다.

| 파일 | 역할 | 조치 |
|---|---|---|
| `worker/routes/cutoffs.ts` | 서빙 | A 캐논 `[1,10,20,50,60]` + `maintOf()` + `pickMaint()` |
| `worker/routes/anchor.ts` | 앵커 배지(유지/조각 판정) | `MAINT_PCTS` = A `[50,60]`, 값 있는 쪽 우선 |
| `worker/lib/league-discovery.ts` | 수집 겨냥 | A `[1,10,20,50]` — 안 오는 60% 자리는 안 찾는다 |
| `desktop/services/league-cutoff-collector.ts` | 앱 내 컷 계산·TTS 문구 | CANON 50·60, `KEEP_PCTS` 배열 |
| UI 4곳(desktop 2 · web 2) + 정적 `web/cutoffs/index.html` | 표시 | 유지 칸 = `50`, `alt: 60` 폴백 |

**설계 원칙 — 서빙은 관대하게, 수집은 현행만.**
서빙(`cutoffs.ts`·`anchor.ts`)은 50·60 둘 다 캐논으로 받고 **현행 50 을 우선**해 한 칸만 내보낸다
(`pickMaint`). 되돌아가도 빈칸이 안 된다. 수집 겨냥(`league-discovery.ts`)은 50 만 — 안 오는
퍼센타일 자리 앵커를 찾는 건 낭비고 `maint = Math.max(canon)` 계산도 50 이어야 맞다.

`Math.max(...canon)` 로 유지선을 잡던 자리가 두 곳(`cutoffs.ts` 순위0 필터, `anchor.ts` keepPct)
있었는데, A 캐논에 60 이 남아 있으면 둘 다 60 을 유지선으로 보고 오늘 값이 전부 새는 구조였다.
그래서 유지선은 `Math.max` 가 아니라 **명시 목록**으로 바꿨다.

### 검증 (typecheck 로는 부족해서 실물로 확인)
`wrangler dev --local` 로 실제 Supabase 를 물려 `/league/cutoffs?fresh=1` 호출.

| 리그 | 유지 50% (수정 후) | 외부 tikdo (17:47Z) |
|---|---|---|
| A1 | 9,067 | 8,689 |
| A2 | 2,564 | 2,329 |
| A3 | 2,968 | 2,944 |

- 어제 칸도 50% 로 통일됐다(A1 67,813 / A2 33,800 / A3 23,612). 08-27 은 50·60 이 함께 온 날인데
  `pickMaint` 가 60 을 빼서 오늘과 같은 기준으로 비교된다.
- **A2 1% 가 살아났다** — 파생값 58,805 이던 칸이 302,133 으로 나온다(tikdo 286,331, 우리 쪽이 더 최신).
  50% 행이 캐논에 들어가 인원 역산 기준이 움직인 것으로 **보이나, 기준값 변화를 직접 확인하진 않았다.**
  같은 시간에 원본 1% 행도 갱신됐으므로 인과는 미확정이다.
- A3 1% 는 묵은 실측(136분)을 버리고 순위표값 100,323 을 냈다 — tikdo 100,324 와 사실상 동일.

### 검증 한계
- `pickMaint` 는 단조 검사보다 **먼저** 돌게 배치했다. 나중에 걸면 버려질 60 이 최장 비증가 부분수열
  계산에 끼어들어 50 의 생사를 좌우한다. 재검증에서 50·60 동시 노출 0건, 역전 0건, today 칸 53 → 56.
- 50·60 이 **함께 온 날(08-27)** 경로는 `yestShape`·`yesterdayFinal` 로만 확인했다(값이 raw 와 일치).
  `estimateLeagueSize` 가 그날 유지 표본을 두 번 세지는지는 직접 안 봤다 — 오늘 데이터로는 그 경로가 안 돈다.

### 남은 것
- **C1 10% 는 여전히 -37%** (우리 13,159 vs tikdo 20,993). 이건 캐논 문제가 아니라
  그 구간 신선 실측이 없어 순위표 파생으로 때우는 문제다. 하베스터 커버리지 건으로 따로 본다.
- 배포 안 함. K 지시 대기.

## 진행 메모 (2026-08-29 04:20 KST) — 남은 오차의 진짜 원인은 "인원(N) 과소추정"

어제 "남은 오차는 전부 측정 시각이 낡아서"라고 적었다. **틀렸다.** 오늘 실측에서 갓 들어온 칸도 벌어진다.

### 기전 규명 (A1, 2026-08-29 19:14Z)
서빙이 실측 없는 칸을 순위표에서 뽑을 때 위치를 `N × p%` 로 잡는다. 그 `N` 을 `estimateLeagueSize` 가
**우리 실측 컷의 순위 위치**에서 역산한다. 오늘 A1 재료는 두 개뿐이었다.

| 재료 | 컷 | 우리 명단에서의 위치 | 역산 N |
|---|---|---|---|
| 20% 실측 | 69,633 | 106 | 530 |
| 50% 실측 | 14,009 | 183 | 366 |

중앙값 → **N = 530** → 10% 자리 = 53위 → 서빙 121,974.
외부(tikdo) 10% = 98,473 인데, 그 점수는 우리 명단에서 **71위** 다 → 외부 기준 **N ≈ 710**.
즉 서빙이 N 을 25% 낮게 잡아 10% 컷이 +24% 부풀었다. 값이 낡아서가 아니라 **자(尺)가 틀렸다.**

### 왜 자가 틀리나 — 순위 명단이 표본이다
`broadcaster_rank_snapshots` 는 리그 전원이 아니라 우리가 긁어 모은 일부다(오늘 A1 kr 269행, 실제 700+ 추정).
명단에 빠진 사람이 있으면 같은 점수의 위치가 실제보다 **앞**으로 잡히고, 그 위치로 역산한 N 은 작아진다.
그 N 으로 다시 위치를 잡으니 오차가 두 번 곱해진다.

### 앵커 순위로 N 을 조이는 방법의 한계 (검토했고 지금은 안 쓴다)
틱톡은 앵커가 아직 못 넘은 문턱만 안내하므로 `0.01·p·N < anchor_rank ≤ N` 이 성립한다.
오늘 A1 행들(1%:48, 10%:89, 20%:233, 50%:343)을 교집합하면 **N ∈ [343, 686]**.
그런데 현재 추정 530 이 이미 이 범위 안이라 **클램프를 넣어도 A1 은 하나도 안 바뀐다.**
게다가 외부가 가리키는 710 은 이 상한(686)을 넘는다 — 앵커 순위도 측정 시각이 제각각이라 경계가 흔들린다.
따라서 "앵커 경계로 N 을 고친다"는 안은 **이번 오차를 못 고친다.** 넣지 않았다.

### 지금 상태에서 고칠 수 있는 것 / 없는 것
- 고쳐짐(이미 워킹트리에 있음, 미배포): A 유지 50% 3칸, A1 1%(정합성 이탈로 **운영은 빈칸**, 수정본은 548,585 / 외부 556,554).
- 못 고침(서빙 밖): 순위 명단 커버리지. 명단이 리그 전원의 일부인 한 N 은 계속 낮게 잡힌다.
  → 진짜 해법은 명단 수집 폭을 늘리거나(리그별 전체 랭킹 페이지네이션), N 을 외부 신호 없이 보정하는 방법을 찾는 것.
  둘 다 이번 변경 범위 밖이라 손대지 않았다.

### 주의 — 칸 단위 비교는 분 단위로 뒤집힌다
같은 대조를 10분 간격으로 두 번 했더니 A1 10% 가 98,090 → 121,974 로 바뀌었고
C5 80% 는 4(이상) → 19(정상), C1 10% 는 7,902 → 14,010(순위표 파생)으로 갈렸다.
**단일 스냅샷의 칸 하나로 판단하지 말 것.** 기전(위 N 계산)처럼 재현 가능한 근거로 판단한다.

## 진행 메모 (2026-08-29 05:40 KST) — 남은 오차는 두 종류다 (신선 대조 A/B)

양쪽 다 같은 시각에 새로 받아 대조했다. 운영 42칸 일치 / 수정본 45칸 일치(A 50% 3칸 채움).

### (1) 유지컷 계통 과대 — 새로 확인된 패턴
컷은 하루 내내 오르기만 하므로 **낡은 값은 작아야 정상**이다. 그런데 유지 구간은 반대로 크다.

| 칸 | 우리 | 외부 | 배수 |
|---|---|---|---|
| B1 70% | 959 | 597 | 1.61 |
| B4 70% | 956 | 721 | 1.33 |
| B5 70% | 508 | 392 | 1.30 |
| C1 80% | 263 | 209 | 1.26 |
| C2 80% | 135 | 116 | 1.16 |
| B3 10% | 31,542 | 25,263 | 1.25 |

전부 `avoidLosingFragments` 변종에서 온 값이고, 계산은 `owner_score + gap_amount` 다.
배수가 1.16~1.61 로 **일정하지 않아 상수 보정이 불가능**하다. 틱톡이 유지 안내에 여유분을 얹는지,
아니면 우리가 안내 시점과 다른 순간의 점수를 더하는지 **구분이 안 된다** — 라이브 관측 없이는 못 가른다.

### (2) 낡은 실측 잔류 — 과소
C5 10% 586(@15:15, 5시간) vs 외부 4,553 (-87%) · C3 10% 10,727(@17:16) vs 15,834 (-32%)
· B3 1% 49,986(@16:53) vs 96,483 (-48%). 그 구간 앵커가 안 잡혀 값이 얼어붙은 경우다. 수집 커버리지 문제.

### 기각된 가설 — `user_league_data.current_percentile` 로 컷을 직접 채우기
"앵커의 현재 상위 q% + 그 사람 점수 = q% 컷" 이면 순위표도 인원 추정도 필요 없다. 그래서 실측 검증했다.
**A1 실데이터가 단조롭지 않다** — q=20 → 118,663 인데 q=50 → 101,328, q=28 → 316,639 이 q=10 → 267,516 보다 크다.
같은 리그 안에서 q 와 점수가 역전한다. 신뢰 못 할 신호라 **쓰지 않기로 했다.** (6시간 내 리그당 24~39행 존재)

### 기각된 가설 — 앵커 순위로 N 조이기
`0.01·p·N < anchor_rank ≤ N` 교집합이 A1 에서 N ∈ [343,686] 인데 현 추정 530 이 이미 그 안이라 **아무것도 안 바뀐다.**
외부가 가리키는 710 은 오히려 상한 밖이다. 코드에 넣지 않았다.

### 그래서 지금 진짜 병목
서빙 로직이 아니라 **입력 두 개**다.
1. 유지컷 gap 의 의미 — 라이브 방송 중 TTLS 화면과 우리가 저장한 값을 나란히 봐야 갈린다.
2. 순위 명단 커버리지 — A1 kr 269행(실제 700+ 추정). 명단이 얕으면 N 도 위치도 같이 틀린다.
둘 다 코드만으로는 못 닫는다. **K 방송 중 실측 캡처가 다음 단계다.**
